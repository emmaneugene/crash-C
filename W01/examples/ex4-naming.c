/*
1. Naming rules for identifiers in C
2. Note that C is a case-sensitive language
3. Use meaningful names for identifiers
4. Identifiers cannot be keywords (e.g. auto, break, case) - but the code intellisense will help you
*/

#include <stdio.h>

int main(void){
    // C allows for letters, digits and underscores
    // first char in the identifier has to be a letter or underscore
}
